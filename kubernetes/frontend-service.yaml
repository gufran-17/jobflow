apiVersion: v1
kind: Service

metadata:
  name: frontend-service
  namespace: jobflow

spec:
  selector:
    app: jobflow-frontend

  ports:
    - protocol: TCP
      port: 5173
      targetPort: 5173

  type: NodePort
